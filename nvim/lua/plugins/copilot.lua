-- GitHub Copilot — optional, requires license
-- Remove `enabled = false` when you have an active Copilot subscription
return {
  {
    "github/copilot.vim",
    enabled = false,
    event = "InsertEnter",
  },
}
